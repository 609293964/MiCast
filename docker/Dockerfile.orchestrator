FROM python:3.11-alpine

WORKDIR /app
RUN pip install --no-cache-dir \
    'fastapi>=0.111' \
    'uvicorn[standard]>=0.30' \
    'pydantic>=2.7' \
    'docker>=7.1'
COPY micast /app/micast

EXPOSE 3100
CMD ["uvicorn", "micast.orchestrator_app:app", "--host", "0.0.0.0", "--port", "3100"]
